[pytest]
# importlib mode lets several skills share a test basename (e.g. every skill has
# its own test_skill_contract.py) without the sys.modules collisions the legacy
# prepend importer would raise.
addopts = --import-mode=importlib
